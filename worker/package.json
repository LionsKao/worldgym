{
  "name": "worldgym-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.120.1"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^1.0.2"
  }
}
